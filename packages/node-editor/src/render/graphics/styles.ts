export interface FillStyles {
	color: string;
	alpha: number;
}

export interface StrokeStyles {
	lineDash: number[];
	lineWidth: number;
	color: string;
	join: CanvasLineJoin;
	cap: CanvasLineCap;
}
